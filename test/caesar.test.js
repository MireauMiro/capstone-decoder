// Write your tests here!

// 

/*
describe("encoding a message", () => {
    it("should encode a message by shifting the letters", () => {

      const actual = caesar("a", 2);
      const expected = "c";
      expect(actual).to.equal(expected);

    });
});*/